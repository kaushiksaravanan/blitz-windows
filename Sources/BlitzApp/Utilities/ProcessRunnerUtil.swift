import Foundation
import BlitzCore

/// Convenience re-export so app code can use ProcessRunner without importing BlitzCore
typealias AppProcessRunner = ProcessRunner
typealias AppManagedProcess = ManagedProcess
