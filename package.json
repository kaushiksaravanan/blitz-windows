{
  "name": "blitz-windows",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix cross-platform/windows-controller",
    "build": "npm run build:all --prefix cross-platform/windows-controller",
    "dist": "npm run dist --prefix cross-platform/windows-controller",
    "start": "npm run start --prefix cross-platform/windows-controller"
  }
}
